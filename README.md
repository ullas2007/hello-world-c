# hello-world-c
# Hello World in C

A simple C program to demonstrate basic compilation and execution.

## How to Compile and Run
Run the following command in your terminal:
`gcc hello.c -o hello && ./hello`

**Student Name:** Ullas M J

## Collaboration Log

* **Pairing Partner:** Sai Sujay (GitHub: @saisujay-prog)
* **Feature Built:** Collaboratively implemented and tested the `greet()` function in `hello.c` using VS Code Live Share.
* **Key Learning:** I learned how GitLens blame annotations make it easy to see exactly who wrote a specific line of code and when it was committed.